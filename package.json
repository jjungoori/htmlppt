{
  "name": "slidecraft",
  "version": "0.0.1",
  "description": "Add PowerPoint-style direct-manipulation UX to arbitrary HTML in the browser.",
  "type": "module",
  "main": "dist/slidecraft.js",
  "module": "dist/slidecraft.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
